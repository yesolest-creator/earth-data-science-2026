# 안내 웹앱 확정 사항 통합본 — index · step1 · step2 · step3

> **이 문서 하나가 확정 사항을 담는 유일한 문서입니다.** (`학생용_프롬프트-확정사항.md`는 별도 유지)
> 개별 대화에서 새로 산출되는 `stepN-확정사항.md`류 파일은 검토 후 이 문서에 병합하고
> 별도로는 남기지 않는다.
>
> **충돌 처리 원칙**: 이 문서 안에서 서술이 어긋나면 **뒤쪽 섹션이 최신**이다.
> **원본 지침(「프로젝트 지침 — 대기·해양 데이터 웹앱」)과 이 문서가 다르면 이 문서가 우선**한다.
> 지침과 달라진 항목은 **§5**에, 과거 결정에서 최종적으로 바뀐 항목은 **§12**에 모여 있다.

**현재 진척**: `index.html`, `step1.html`, `step2.html`, `step3.html` 완성. `step5.html` 1차 작성 완료(§13, 사용자 검토 대기). `step4`, `step6` 미착수.

> `step5.html` 확정 사항은 문서 순서상 뒤쪽인 **§13**에 있다. 기존 절 번호(§5~§12)를 그대로 유지하기 위해 새로 추가했다.

---

## 0. 작업 프로세스 원칙 (모든 대화에 공통 — 항상 준수)

1. **사용자의 명시적 지시가 있을 때까지 코딩을 시작하지 않는다.**
   논의가 아무리 구체적이어도 "확정하자" / "코드 짜줘" 같은 지시 전에는 html·css·js를 만들거나
   고치지 않는다. 그 전까지는 초안·선택지·정리만 텍스트로 제시하고 검토를 기다린다.
   → step2에서 첫 프로토타입을 통째로 폐기한 이력이 있어, 이 원칙은 계속 유지한다.
2. **페이지별로 새 대화창에서 작업한다.** 각 대화에서 정해진 내용은 반드시 `stepN-확정사항.md`로
   기록해 프로젝트 지식에 등록한다. 기존 문서와 어긋나는 결정이 생기면 그 사실과 수정분을
   명시해 문서 간 불일치를 남기지 않는다.
3. 새 대화 시작 문구 예시:
   > "지침 문서와 확정사항 통합본을 참고해서 step4.html 구성안을 제안해줘.
   > `styles.css`·`script.js`는 그대로 재사용. **내가 확정 지시할 때까지 코드는 짜지 마.**"

---

## 1. 파일 구성 · 배포

| 파일 | 상태 | 비고 |
|---|---|---|
| `index.html` | 완성 | 허브 |
| `step1.html` | 완성 | 목표 및 평가 항목 |
| `step2.html` | 완성 | 데이터 분석 방법 |
| `step3.html` | 완성 | 웹앱 제작 연습 |
| `step4.html` | 미착수 | `styles.css`에 35번 섹션(주제 카드·프롬프트 입력칸)만 선반영되어 있고, 페이지·JS는 아직 없음 — §11-11 참고 |
| `step5.html` | 1차 작성 완료(§13) | 기획안 작성 — 사용자 검토·확정 대기 |
| `step6.html` | 미착수 | |
| `styles.css` | 섹션 1~36 | 전 페이지 공용 |
| `script.js` | step1·2·3 블록 | 전 페이지 공용. step5는 기존 함수(`initCopyButtons()`)와 네이티브 `<details>`만 사용해 새 블록 없음 |

- **배포 시 위 파일 전부를 같은 폴더에 함께 업로드**한다. 상대경로 `<link rel="stylesheet" href="styles.css">`로
  연결되어 있다. 폰트는 CDN `@import`라 별도 업로드 불필요.
- **누적 원칙**: 지금까지 기존 토큰·클래스를 변경·삭제한 적이 한 번도 없다. 새 step을 만들 때도
  **기존 것은 건드리지 않고 뒤에 추가만** 한다. 그래야 앞 페이지가 그대로 동작한다.

---

## 2. 전 페이지 공통 규칙 (step1~6 동일하게 유지할 것)

### 2-1. 헤더 — `.topbar` + `.steptrack`

**step1.html의 헤더 마크업을 그대로 복사하고 `aria-current="step"` 위치만 옮긴다.**

- 상단 줄
  - 좌측 `.brand`: `과학자는 데이터로 의사소통한다` (작은 글씨, `데이터`에 `.accent`로 tide 컬러).
    클릭 시 `index.html`로 이동 → 별도 "메인으로" 버튼은 중복이라 두지 않는다.
  - 우측 `.kicker`: `지구과학 · 대기와 해양의 상호작용` (메인 페이지 킥커와 동일 문구, mono 톤).
    600px 이하에서 숨김.
- 아래 줄 `.steptrack`: STEP 01~06 가로 타임라인, 각 step 페이지로 링크.
  `STEP 0N`(mono, 위) + 페이지 제목(아래) 2줄 구조.
  - 현재 페이지: `aria-current="step"` → 번호 tide + 제목 흰색 700 + 하단 tide 2px 밑줄
  - 다른 페이지: 흰색 저투명도(30%/46%), hover 시 밝아짐
  - 배경에 masthead와 동일한 데이터 격자 패턴을 옅게 깔아 톤을 맞춤
  - 좁은 화면에서 가로 스크롤, 인쇄 시 숨김
- 타임라인 라벨은 지침 6번 제목 그대로:
  `목표 및 평가 항목` / `데이터 분석 방법` / `웹앱 제작 연습` / `데이터 활용 가이드` /
  `기획안 작성` / `웹앱 구현 및 점검`
- `STEP 1 / 6` 형식의 `.crumb`는 **쓰지 않는다**(타임라인이 위치를 보여주므로 제거).
  클래스 자체는 `styles.css`에 남아 있음.

### 2-2. 기타 공통 요소

- 탭 제목 형식: `{페이지 제목} · 대기·해양 데이터 프로젝트`
- `.pagehead`: h1 페이지 제목 + `읽는 데 약 N분` (`.readtime`)
- **`.savenote` — 문구 고정, 전 페이지 동일하게 배치**
  > 이 페이지에 입력한 내용은 저장되지 않습니다. 새로고침하거나 페이지를 벗어나면 사라지니, 필요한 내용은 따로 기록해 두세요.
- 하단 `.pagenav`: `← 이전 단계 제목` / `메인으로` / `다음 단계 제목 →`

### 2-3. 용어 규칙

- 지침에서는 교사용 구분으로 "안내 웹앱 / 학생 웹앱"을 쓰지만,
  **학생에게 보이는 화면 문구에서는 "학생 웹앱"이라 쓰지 않고 그냥 `웹앱`이라고 쓴다.**
- 문서·대화에서는 기존대로 안내 웹앱 / 학생 웹앱 구분 유지.
- **"오픈소스"라는 표현을 쓰지 않는다.** step3 실제 데이터는 비밀번호로 막혀 있어
  "누구나 자유롭게"라는 오픈소스 개념과 반대다.

### 2-4. 문체·서술 규칙 (step2에서 확정, 이후 전 페이지 적용)

- **친절한 비유를 늘어놓지 않는다.** 과학고 1학년은 1학기 심화실험에서 회귀를 이미 접했다고 전제한다.
  "복잡해 보이지만 하는 일은 단순합니다" 같은 안심시키는 문장은 쓰지 않는다.
- **어떤 함수로 계산하는지는 반드시 쓴다.** 정량 분석이 이 프로젝트의 핵심이므로 계산 도구 명시는
  교육적으로 협상 대상이 아니다.
- 수식은 제시하되 유도 과정은 생략한다.
- 루브릭·체크리스트 서술은 `~하였다` 체로 통일하고, **도달했을 때의 모습을 완성형 문장으로** 쓴다.

---

## 3. 디자인 시스템 — 구체화된 부분

원본 지침 7번의 색상 토큰·클래스 체계를 그대로 따르되, 구현 과정에서 아래가 정해졌다.

### 3-1. 폰트

```css
--font-body: 'Pretendard Variable', -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
--font-heading: 'Noto Serif KR', serif;
--font-mono: 'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, monospace;
```

- CDN import 3종은 `styles.css` 최상단에 위치.
- 적용 규칙: `h1~h4`, `.note`, 카드 제목, 핵심 질문 강조문(`.q em`)에 `--font-heading`.
  본문·설명·배지·소요시간은 `--font-body` / `--font-mono`.
- **Noto Serif KR은 600·700만 로드**되어 있다 → 800 이상 굵기, -.03em 이하 음수 자간 금지
  (현재 700 / -.01em 선에서 사용).

### 3-2. 한글 줄바꿈 (전역)

- `body`에 `word-break: keep-all;` 적용 완료. 한글이 음절 단위로 끊겨
  "의사소 / 통한다"처럼 보이는 문제를 막는다. **새 페이지에서 별도 조치 불필요.**
- 좁은 컨테이너에 긴 한글 헤드라인을 넣을 때는 실제 줄바꿈 위치를 확인할 것.

### 3-3. `styles.css` 섹션 목록 (1~35)

| # | 섹션 | 도입 |
|---|---|---|
| 1 | 토큰 | index |
| 2 | 리셋·기본 (`word-break:keep-all` 포함) | index |
| 3 | `.topbar` (`.brand`, `.accent`, `.kicker` 포함) | index / step1 확장 |
| 4 | `.masthead` | index |
| 5 | 섹션 골격 | index |
| 6 | `.hubgrid` / `.hubcard` (`.pending`, `.cols-2` 포함) | index |
| 7 | `.card` | index |
| 8 | `.badge` (`.b-todo`/`.b-live`/`.b-open`/`.b-done`) | index |
| 9 | `.note` (`.note.tide`) | index |
| 10 | `.btn` (+ `.btn[hidden]{display:none;}`) | index / step2 보강 |
| 11 | `.chartbox` / `.toggle` | index |
| 12 | `.check` | index |
| 13 | `.src` | index |
| 14 | `.pagenav` | index |
| 15 | `.pagehead` / `.stepsection` / `.lead` | step1 |
| 16 | `.savenote` | step1 |
| 17 | `.tablewrap` / `.table` (`.num-col`·`.when`·`.due`·`.warn`·`.lines`·`tr.total`) | step1 |
| 18 | `.selectbar` | step1 |
| 19 | `.flowsteps` | step2 |
| 20 | `.srclist` / `.src-desc` / `.codeblock`(`.copy`,`.cm`,`.st`,`.kw`) / `.datapeek`(`.peekhead`) / `.statrow`(`.k`,`.v`) / `.subhead` | step2 |
| 21 | `.loader` (`.lfield`,`.lbtns`,`.lstate`) / `.srcflag` | step2 |
| 22 | 차트 SVG (`.chart-svg`, `.chartlegend`, `.chartpanel`, `.chart-cap`, `.chart-empty`) | step2 |
| 23 | `.formula` (`.frac`,`.top`,`.bot`,`.sqrt`,`.eq`) | step2 |
| 24 | `.methodgrid`(`.mhead`), `.badge.b-lv1/b-lv2/b-lv3` | step2 |
| 25 | `.quiz` (`.qlabel`,`.qtext`,`.choices`,`.fb`) | step2 |
| 26 | 반응형 | index(이동) |
| 27 | 모션 최소화 | index(이동) |
| 28 | 인쇄 | index(이동) |
| 29 | step3 — 데이터 선택(`.datapick`) · 프롬프트 빌더 | step3 |
| 30 | step3 — 04 결과 예시 막대그래프 · 05 힌트 텍스트 | step3 |
| 31 | step3 — 길어진 프롬프트 블록 스크롤 처리 | step3 |
| 32 | step3 03 — 응답 분포 누적 막대(`.distbar`,`.distseg`,`.distlegend`) · AI 분석 프롬프트 블록(`.analyzeblock`) | step3(수정) |
| 33 | step3 02 — 비밀번호 입력창 축소, CSV 다운로드 버튼을 확인 버튼 옆으로 이동(`.pwrow`) | step3(수정) |
| 34 | step3 04 — 그래프 박스 안 정리 문장(`.chart-note`) | step3(수정) |
| 35 | step3 05 — "공통 규칙만 복사" 블록(`.rulecopy`) | step3(수정) |
| — | *(주의 — step5 작업 중 발견)* 위 35번 행은 `.rulecopy` 클래스를 실제 파일에서 찾을 수 없다(기존 클래스 재사용으로 구현했거나 문서만 남고 코드는 없었던 것으로 보임). 반면 실제 파일의 섹션 35는 **`.topicgrid`/`.topiccard`/`.srctable`/`.s4fields`(step4 주제 카드·데이터 출처 표·프롬프트 입력칸)**로, 문서(§1, §9-2)의 "step4 미착수" 서술과 달리 CSS만 먼저 만들어져 있다. `script.js`에는 대응 함수가 없고 `step4.html` 파일도 없다 — 실제 착수 전 밑작업만 된 상태로 보인다. step4를 시작할 때 이 CSS를 그대로 쓸지 확인 필요(§11-11). | — |
| 36 | step5 — `.msgframe`(메시지 설계 4단계 입력) / `.exwrap`(프롬프트 2단계 "작성 예시 보기" 토글, 네이티브 `<details>` 사용) | step5 |

**새 섹션은 항상 뒤에 추가하고, 26~28(반응형·모션·인쇄)은 지금처럼 중간에 두어도 무방하다.**

### 3-4. 반응형 기준점

- 900px 이하: 3열 → 2열 / 600px 이하: 2열 → 1열, 여백·폰트 축소
- `prefers-reduced-motion`, 인쇄용 스타일 반영됨. 인쇄 시 `.savenote`, `.selectbar`, `.pagenav`, `.steptrack` 숨김.
- `.table`은 `min-width:520px` + `.tablewrap` 가로 스크롤로 모바일 대응.

### 3-5. 함정 (실제로 사고 났던 것)

- **`hidden` 속성**: `.btn`의 `display:inline-flex`가 `hidden`을 이겨버린다 →
  `.btn[hidden]{display:none;}` 필요. 다른 컴포넌트에 `display`를 줄 때도 같은 함정 주의.
- **SVG는 CSS를 믿지 않는다** (§4-3 참고).
- **SVG `rect`에 `fill`을 지정하지 않으면 기본값이 검정**이라 화면이 새까매진다. 반드시 명시할 것.

---

## 4. `script.js` 구조

`script.js`는 **step1에서 생성**됐다. step별 블록이 순서대로 누적되어 있고, 앞 블록은 손대지 않는다.

### 4-1. [교사 수정 구역] — 배포 전 확인

| 상수 | 위치 | 내용 |
|---|---|---|
| `SUBMIT_SCHEDULE` | step1 블록 | 반별 제출 기한. 현재 전부 `null`(화면에 "미정") |
| `STEP2_URL` | step2 블록 | GISTEMP·마우나로아 CSV 주소 2개 |
| `REAL_APPS_SCRIPT_URL` | step3 블록 | Apps Script `/exec` 주소 |
| `EXAMPLE_CSV_URL` | step3 블록 | 참고용, **프롬프트에서 미사용** |

**`SUBMIT_SCHEDULE` 구조** — `default`(미선택)는 범위, 각 반은 날짜 하나로 **형식이 다르다.**

```js
const SUBMIT_SCHEDULE = {
  default: {                                  // 반 미선택 → 범위(start ~ end)
    plan:   { start: null, end: null },
    report: { start: null, end: null },
    webapp: { start: null, end: null }
  },
  "1": { plan: null, report: null, webapp: null },   // 반 선택 → 날짜 하나
  ...
  "8": { plan: null, report: null, webapp: null }
};
```

- 날짜 문자열은 `"YYYY-MM-DD"` → 화면에는 `9월 15일` 형식. 값이 없으면 `미정`.
- 키: `plan` = 기획안 PDF, `report` = 결과 보고서 PDF, `webapp` = 웹앱 결과물.
- HTML 연결: 기한 셀에 `data-due="plan|report|webapp"` 속성만 달면 자동 갱신.

### 4-2. step2 블록 함수군

| 그룹 | 함수 |
|---|---|
| 통계 | `s2mean` `s2corr` `s2solve` `s2polyfit` `s2polyval` `s2expand` `s2r2` `s2fmt` |
| CSV | `s2parseCSV` `s2cleanTemp` `s2cleanCO2` `s2merge` |
| 차트 helper | `s2ticks` `s2panel` `s2path` `s2line` `s2dots` `s2bars` `s2text` `s2svg` `s2curve` |
| 04 차트 | `s2chartCorrTime` `s2chartCorrScatter` |
| 05 차트 | `s2chartRegression` |
| 07 차트 | `s2demoLine` `s2mistakeOutlier` `s2mistakeAxis` `s2mistakeSlice` `s2mistakeGeneral` |
| 렌더 | `s2renderPeek` `s2renderTable` `s2renderStats` `s2renderCorr` `s2renderReg` `s2renderAll` `s2useFallback` |
| 초기화 | `initClassSelect` `initStep2Loader` `initToggles` `initQuiz` `initCopyButtons` `initMistakeCharts` |

- `s2polyfit`은 **x를 평균만큼 중심화해서** 정규방정식을 푼다. 연도(1880~2025)를 그대로 8차까지
  올리면 수치적으로 무너지기 때문. 화면에 원 좌표계 계수(a·b)를 보일 때는 `s2expand()`로 되돌린다.

### 4-3. SVG 차트는 CSS에 의존하지 않는다 (중요 — step4~6에서도 준수)

**선 색·굵기·채움·글자색을 CSS 클래스가 아니라 SVG 속성으로 직접 지정한다.**
`styles.css`가 구버전으로 로드되자 회귀선이 통째로 사라지고(`stroke` 기본값 `none`),
예측 구간 음영이 검게 나오는(`fill` 기본값 검정) 사고가 있었다.

- 색은 `S2C` 상수에 모아둠(`S2C.temp`, `S2C.fit`, `S2C.band` 등). 색 변경은 여기만.
- `s2line(점목록, 패널, 색, 굵기, 점선여부)` / `s2dots(...)` / `s2bars(...)` / `s2text(...)` —
  모두 속성을 직접 붙여 내보낸다. 클래스명도 함께 남기므로 CSS 덮어쓰기는 여전히 가능.

### 4-4. 재사용 가능한 범용 부품 (step4~6에서 그대로 쓸 것)

- **`initToggles()`** — `[data-toggle]` 안의 `button[aria-controls]`을 탭으로 만든다.
  `data-render="함수명"`을 달면 그 탭이 처음 열릴 때 `.chart-slot`에 지연 렌더링.
- **`initQuiz()`** — `.quiz` 안 `.choices button`에 `data-correct="1"` + `data-fb="피드백"`만 달면 동작.
  HTML만 쓰면 되고 JS는 건드릴 필요 없다.
- **`initCopyButtons()`** — `.codeblock .copy` 버튼을 자동 연결한다.
- **`copyTextToClipboard(text)`** — 복사 버튼이 공유하는 공용 함수. `navigator.clipboard.writeText`를
  먼저 시도하고, 없거나 실패하면 숨긴 `<textarea>` + `document.execCommand("copy")`로 대체한다.
  `navigator.clipboard`가 막힌 환경(HTML 파일을 직접 열었을 때, OneDrive 미리보기 등 보안
  컨텍스트가 아닌 경우)에서도 동작해야 하므로, **새 복사 버튼을 만들 때는 `navigator.clipboard`를
  직접 부르지 말고 반드시 이 함수를 통할 것.** 성공/실패를 `.then()`/`.catch()`로 받아 상태
  문구까지 갱신하는 패턴은 `step3AttachCopy()` 참고.
- **`s2panel()` + `s2line()`/`s2dots()`/`s2bars()`/`s2text()` + `s2svg()`** —
  외부 라이브러리 없이 SVG 차트를 만드는 최소 도구. 축·격자·눈금·제목까지 처리한다.

### 4-5. 수식 렌더링 — MathJax·KaTeX 미사용

`.formula` 클래스(CSS 분수 + `√` 오버라인 + 유니코드 아래첨자)로 처리. 필요한 수식이 r 공식
하나뿐이라 250KB 의존성을 붙일 이유가 없다고 판단.

```html
<div class="formula">
  <span class="eq">r =</span>
  <span class="frac">
    <span class="top">Σ(xᵢ − x̄)(yᵢ − ȳ)</span>
    <span class="bot"><span class="sqrt">Σ(xᵢ − x̄)²</span> <span class="sqrt">Σ(yᵢ − ȳ)²</span></span>
  </span>
</div>
```

- **√는 CSS 가상요소가 아니라 실제 문자로 HTML에 넣는다**(가상요소로 넣었더니 CSS가 어긋났을 때
  근호가 통째로 사라졌다). 같은 이유로 `.formula` 안 스타일은 인라인으로도 한 번 더 지정해 둠.
- **행렬·적분 등 복잡한 수식이 필요해지면 그때 KaTeX 도입을 재검토한다.**

---

## 5. 원본 지침과 달라진 확정 사항 ⚠️

> 지침 문서를 갱신하지 않았다면, 아래 4건은 **이 문서 기준으로 읽어야 한다.**

### 5-1. 루브릭 — 5개 영역, 15점 만점 (step1 확정)

지침 3번의 `과학적 타당성 / 기획의 구체성 / 데이터 리터러시 / 의사소통 효과성 / 완성도`에서
`완성도` → **`웹앱 완성도`**로 개칭한다. 지침에는 없던 배점을 아래와 같이 신설했다.

**표 순서는 학생 작업 타임라인 순**(기획 → 분석 → 결론 → 전달 → 제작)으로 배열한다.

| 영역 | 배점 | 상세 설명 |
|---|---|---|
| 기획의 구체성 | 4 | 관찰·연결·임팩트·요청 4단계를 모두 채웠다. / 각 단계마다 구체적인 데이터와 근거를 사용하였다. / 실행 가능한 수준까지 기획을 구체화하였다. |
| 데이터 리터러시 | 3 | 데이터 출처와 처리 과정을 투명하게 제시하였다. / 이상치·표본 왜곡 등 통계적 오류 없이 분석하였다. |
| 과학적 타당성 | 3 | 데이터가 뒷받침하는 범위 안에서 결론을 도출하였다. / 근거와 주장 사이에 논리적 비약이나 과잉 일반화가 없었다. |
| 의사소통 효과성 | 3 | 비전공 일반 대중이 이해할 수 있는 언어와 시각화를 사용하였다. / 핵심 메시지를 명료하고 과장 없이 전달하였다. |
| 웹앱 완성도 | 2 | 웹앱이 오류 없이 작동하였다. / 기본적인 완성 수준을 갖추었다. |
| **합계** | **15** | |

**step5·step6에서 루브릭을 참조할 때는 지침 원문이 아니라 위 5개 항목·배점(15점 만점)을 기준으로 삼을 것.**

### 5-2. 제출물 표기 (step1 확정)

- 컬럼: 지침 4번의 `제출물 / 시점 / 역할` → **`제출물 / 시점 / 설명 / 제출 기한`**
- 명칭: 지침의 `학생 웹앱 파일 일체` → **`웹앱 결과물`**
- GRASPS "기준" 항목은 별도 문구를 만들지 않고 **루브릭으로 넘기는 연계 방식**으로 처리.

### 5-3. 지침 5(데이터·보안 원칙) — step3 한정 예외

**적용 범위는 안내 웹앱(`step3.html`/`script.js`)의 02 인증 로직 하나뿐이다.** step5·step6의
정식 학생 웹앱에는 지침 5 원문 5개 항목이 수정 없이 그대로 적용된다(그 결과물이 실제로
GitHub Pages에 배포되므로). `학생용_프롬프트-확정사항.md`는 이 원칙으로 이미 작성돼 있다.

| # | 항목 | 안내 웹앱 02 인증 로직 | 05 공통 규칙(학생이 AI에게 전달하는 프롬프트) |
|---|---|---|---|
| 1 | 방문자 개인정보 수집 금지 | ✅ | ✅ |
| 2 | 로그인·이름·이메일 입력, 위치추적 금지 | ✅ | ✅ |
| 3 | API 키·토큰·비밀번호 하드코딩 금지 | ✅(비밀번호는 학생이 매번 직접 입력 — 코드에 하드코딩되지 않음) | ✅ |
| 4 | 인증키 필요 외부 API 금지, 공개 CSV·내장 정적 데이터만 | ❌ **예외** — 비밀번호를 확인하려면 Apps Script를 호출해야 함 | ✅ — `real_data.csv`를 정적으로 내장하고, "외부 링크나 API를 호출하지 않는다"는 문장을 데이터 출처 지시에 명시 |
| 5 | localStorage·sessionStorage 등 브라우저 저장 금지 | ✅ | ✅ |

**예외가 필요한 이유(4번 한정)**: 02섹션은 "비밀번호로 실제 데이터를 확인해본다"는 연습 자체가
목적이라, 인증이 필요한 외부 호출(Apps Script)을 피할 수 없다. 원본 스프레드시트 링크는
Apps Script 프록시 뒤에 가려져 어디에도 노출되지 않는다.

**미해결 리스크**: 07섹션 방침대로 실제 데이터(전교생 설문 응답)로 만든 웹앱이 전교생에게
게시되면, 웹앱 코드에 정적으로 내장된 CSV 원본도 함께 노출된다. 이름·이메일 등 신원 정보는
없지만, 실제 응답 데이터가 공개된다는 점 자체가 별도 정책 결정이 필요한 사안이다 — **§11-1 참고.**

### 5-4. 메시지 설계 프레임워크 도입 시점

관찰-연결-임팩트-요청 4단계는 **step4(데이터 활용 가이드)에서 처음 소개**한다.
step2의 08 섹션에서 "기획안 작성에서 이 4단계로 설계한다"고 **예고만** 해두었다.
step3에서는 다루지 않았다.

### 5-5. 모둠 → 개별 수행평가

지침 4번의 `모둠 구성: 2~3인 모둠`은 **개별 수행평가로 대체한다.** 160명 전원이 각자 하나의
웹앱을 기획·제작·제출한다.

- **`모둠 협업 도구: MS Teams 활용`** 조항은 **개별 제출·소통 도구**로 재정의한다.
- `group_role`(모둠 과제 선호 역할) 설문 문항은 프로젝트 팀 편성과 무관한 개인 성향 설문이므로
  그대로 둔다.
- `outputs/` 폴더명 규칙, 주제 중복 제한 여부는 **§11 참고.**

---

## 6. index.html (허브) — 확정 내용

### 6-1. masthead

지침 3번 **핵심 질문 원문을 그대로 인용**해, "이 활동이 왜 필요한가"를 최상단에서 먼저 짚는다.
(백워드 설계상 최종 목표를 맨 앞에 두는 것이 중요하다는 판단. 새 문구를 창작하지 않음.)

**구성 순서 (고정)**
1. 킥커: `지구과학 · 대기와 해양의 상호작용`
2. 핵심 메시지(변경 금지): `과학자는 데이터로 의사소통한다.`
3. 핵심 질문 2개 (지침 3번 원문 그대로)
   - "데이터는 그 자체로는 아무 말도 하지 않는다. 그 안에서 의미를 찾아내는 것이 과학자의 일이다. **나는 무엇을 찾아냈는가?**"
   - "찾아낸 의미를 오해 없이 전달하는 것 또한 과학자의 몫이다. **나는 이를 위해 어떤 선택을 해야 하는가?**"
4. 연결 문장: `이 두 질문에 답하는 과정이 여러분이 지금부터 4차시 동안 할 일입니다.`

**영속적 이해(4줄)는 index에 넣지 않고 step1으로 이관**했다.

### 6-2. 카드 — 준비 단계 (번호 있음)

| # | 제목(고정) | 한 줄 설명 | 소요시간 |
|---|---|---|---|
| 1 | 목표 및 평가 항목 | 이 프로젝트에서 어떻게 평가받는지 확인하세요. | 약 5분 |
| 2 | 데이터 분석 방법 | 데이터를 어떻게 정량적으로 분석하는지 알아봅니다. | 약 15분 |
| 3 | 웹앱 제작 연습 | 실제 제작에 앞서 AI 코딩 도구로 간단히 웹앱을 만들어봅니다. | 약 20분 |
| 4 | 데이터 활용 가이드 | 주제별로 활용 가능한 공공데이터 소스를 안내합니다. | 약 10분 |
| 5 | 기획안 작성 | 기획안을 구체적으로 작성하고 웹앱 제작 프롬프트를 생성합니다. | 약 50분 |
| 6 | 웹앱 구현 및 점검 | 학생 웹앱을 만들고 제출 전 점검합니다. | 약 50분 |

- 권장 순서는 화살표 그래픽 없이 섹션 헤드에 한 줄로:
  `1 → 6 순서로 보시길 권장하지만, 필요한 단계부터 바로 확인하셔도 됩니다.`
- 진행상황(N/6 완료) 표시 없음 — 지침상 명시적 배제.

### 6-3. 카드 — 결과 공유 (번호 없음, 링크 없음, 클릭 불가)

| 제목(고정) | 설명 | 배지 |
|---|---|---|
| 전체 웹앱 결과물 보기 | 모든 학생 제출 완료 후 공개됩니다. | `.b-todo` 공개 예정 |
| 웹앱 사용자 평가 제출 | 모든 학생 제출 완료 후 공개됩니다. | `.b-todo` 공개 예정 |

- 별도 섹션(`결과 공유`)에 `.hubgrid.cols-2` + `.hubcard.pending`(점선 테두리)로 배치.
- 번호를 매기지 않은 이유: "진행 안내"가 아니라 "완료 후 결과"라 step1~6과 다른 카테고리.

---

## 7. step1.html — 목표 및 평가 항목

**페이지 목적**: "왜 이런 방식으로 평가받는가"에 학생이 납득하고 프로젝트를 시작하게 하는 페이지.
소요시간은 내용이 길어져도 **"약 5분" 유지**(조정하지 않기로 함).
`.pagenav`: `이전 단계 없음`(disabled) / `메인으로` / `데이터 분석 방법 →`

### 섹션 01 — 왜 이 방식으로 평가하나요

리드: `이 프로젝트가 지키는 네 가지 원칙입니다.`
지침 3번 **영속적 이해 4줄을 원문 그대로**(다듬은 버전을 검토했으나 원문 채택). `.note.tide` 4개.

1. 데이터 없이 주장만 하는 것은 과학적 의사소통이 아니다.
2. 데이터가 있어도 해석하지 않으면, 아무 의미 없는 숫자 배열일 뿐이다.
3. 해석을 하더라도 성급하게 판단하면 잘못된 결론에 이를 수 있다.
4. 데이터를 신중하게 해석하고, 그 결과를 정직하게 전달하는 것 — 그것이 좋은 과학 커뮤니케이션이다.

### 섹션 02 — 이번 프로젝트, 한눈에 보기 (GRASPS)

| 항목 | 내용 |
|---|---|
| 목표 | 대기·해양 공공데이터를 정량 분석해, 대중에게 신뢰성 있게 전달하는 웹앱을 기획합니다. |
| 역할 | 데이터 커뮤니케이터 |
| 대상 | 이 주제를 잘 모르는 일반 대중 |
| 결과물 | 기획안(PDF) → 웹앱 제작 → 결과 보고서(PDF) |
| 기준 | 아래 평가 루브릭(5개 영역, 15점 만점)을 기준으로 평가합니다. |

### 섹션 03 — 평가 루브릭 · 15점 만점

리드: `다섯 개 영역으로 나누어 평가합니다. 웹앱 자체의 완성도보다 기획 과정을 더 크게 봅니다.`
→ 표는 **§5-1** 참조.

### 섹션 04 — 제출물 안내

리드: `반을 선택하면 제출 기한이 표시됩니다.`
드롭다운 `#class-select`: 기본값 `반 선택`, 옵션 `1반`~`8반`(8개 학급)

| 제출물 | 시점 | 설명 | 제출 기한 |
|---|---|---|---|
| 기획안 PDF | 2차시 | 기획 구체성·타당성 평가, 교사 피드백의 근거로 활용됩니다. | 스크립트 연동 |
| 결과 보고서 PDF | 4차시 | 분석 결과와 웹앱에 기획 의도가 반영되었는지를 확인합니다. 기술적 문제가 생겼을 때 평가 백업 자료로도 쓰입니다. | 스크립트 연동 |
| 웹앱 결과물 | 4차시 | 코드 일체를 제출합니다. 배포는 제출 후 교사가 진행합니다. **온라인에 배포되므로, 이름·연락처 등 개인정보를 코드에 절대 포함하지 마세요.**(`.warn`) | 스크립트 연동 |

---

## 8. step2.html — 데이터 분석 방법

**페이지 목적**: 상관분석 심화가 아니라 **데이터 분석 전반의 기초 리터러시.**
상관/인과는 그중 가장 기본적인 사례일 뿐이다. (첫 프로토타입이 "상관분석 딥다이브"로 흘러 폐기된 것이
이 판단의 배경.) 8개 방법을 난이도별로 나눠 **초급 2개만 자세히**, 중급·심화 6개는 이름과 쓰임새만.
소요시간 `약 15분`. `.pagenav`: `← 목표 및 평가 항목` / `메인으로` / `웹앱 제작 연습 →`

### 8-1. 섹션 구성 (8개)

| # | 섹션 헤드 | 핵심 내용 |
|---|---|---|
| 01 | 오늘 다룰 지도 | 8개 방법 매트릭스 표(난이도 배지) — 전체 지도를 맨 앞에 |
| 02 | 데이터 분석이 하는 일 | 원자료의 무의미함 + 4단계(불러오기·정리하기·패턴찾기·해석하기) |
| 03 | 데이터는 어떻게 불러오는가 | 자료 소개 + 실시간 불러오기 인터랙션 + 전처리 + 정리된 표 |
| 04 | 초급 ① 상관분석 | 차트 토글 2종 + r 설명 + r 계산식 + 확인문제 + **상관≠인과** |
| 05 | 초급 ② 회귀 | 회귀 차트(1차·2차 + 예측구간) + R² + 차수 선택 + 한계 + 함수 + 확인문제 |
| 06 | 더 나아가면 | 중급·심화 6개 카드 개요 |
| 07 | 자주 하는 실수 | 체크리스트 4개 + 4탭 예시 그래프(전부 임의 데이터) + 확인문제 |
| 08 | 다음 단계에서 할 일 | 정리 + step3·4·5 연결 |

### 8-2. 사용 자료 2종

```python
# 1) 전지구 평균기온 편차 (GISTEMP 기준만 골라내기)
temp = pd.read_csv("https://raw.githubusercontent.com/datasets/global-temp/main/data/annual.csv")
temp = temp[temp['Source'] == 'GISTEMP'][['Year','Mean']]

# 2) 대기 중 CO2 농도 (마우나로아 관측소)
co2 = pd.read_csv("https://raw.githubusercontent.com/datasets/co2-ppm/main/data/co2-annmean-mlo.csv")
co2 = co2[['Year','Mean']]
```

- **출처 표기 확정 문구**: "공개 데이터 저장소(GitHub datasets)가 미러링한 자료로, 원 출처는
  NASA GISTEMP / 하와이 마우나로아 관측소(NOAA)입니다."
  → **OWID가 아니다.** 다른 페이지에서 인용할 때 주의.
- **기온 파일에 GCAG·GISTEMP 두 출처가 섞여 있다는 사실 자체를 전처리 교육 소재로 쓴다.**
  이 파일을 다른 페이지에서 쓸 때도 필터링 필요성을 반드시 언급할 것.

**실제 계산값 (2025년 자료 기준)** — 화면에는 실시간 계산값이 표시되며 **HTML에 하드코딩되어
있지 않다.** 문서·수업설계안에 인용할 때만 참고.

| 항목 | 값 |
|---|---|
| 병합 구간 | 1959–2025 (67개 연도) |
| 기온 자료 전체 | 1880–2025 (146개 연도) |
| r (CO₂ vs 기온) | 0.97 |
| 1차 회귀 a / b | 0.0083 / −16.12 |
| 1차 R² / 2050년 예측 | 0.77 / 0.89℃ |
| 2차 R² / 2050년 예측 | 0.91 / 1.63℃ |
| 8차 R² | 0.94 |

### 8-3. 계산 도구 명시 (문체 규칙 §2-4의 구체 사례)

- 상관분석: `numpy.corrcoef()` / 엑셀 `CORREL()`
- 회귀: `numpy.polyfit(x, y, 차수)` / 엑셀 `SLOPE()`·`INTERCEPT()`(선형만),
  다항은 차트 "추세선 추가"
- ⚠️ `SLOPE()`/`INTERCEPT()`는 **선형 전용**이라 다항에 쓸 수 없다. 둘을 한 문장에 뭉뚱그리지 말 것.
- 사이킷런은 언급하지 않는다(05는 `numpy.polyfit()` 하나로 해결됨). 중급·심화를 실제로 다루는
  자리에서는 다시 등장 가능.

### 8-4. 03 섹션 인터랙션 사양

1. URL 입력칸 2개는 **비어 있는 상태로 시작.**
2. **`채우기`** 버튼 → 두 주소가 채워진다. (미리 채워두지 않는 것이 요점 — 학생이 "데이터에는
   주소가 있다"를 직접 보게 하려는 의도)
3. **`불러오기`** 버튼 → 브라우저가 실제로 두 CSV를 `fetch`.
4. 성공 시: 원자료 앞 5줄(`.datapeek`) + 정리된 표(마지막 6개 연도) + 요약 수치(`.statrow`)가
   나타나고, **04·05의 그래프와 숫자가 전부 이 자료로 채워진다.**
5. 실패 시: `지금은 인터넷 연결 문제로 데이터를 불러오지 못했습니다.` + **`저장된 데이터로 보기`**
   버튼. 누르면 내장 저장본으로 동일하게 채워지되 배지가 **`저장된 자료 기준`**으로 바뀐다.
   **몰래 대체하지 않고 항상 어떤 자료인지 표시한다.**
6. 자료 로드 전 04·05 차트 자리에는 `위 03에서 자료를 불러오면 그래프가 그려집니다.` 안내.
   (07 섹션 차트는 임의 데이터라 자료 없이도 바로 보인다.)

### 8-5. 07 섹션 사양

- 체크리스트 4개: 이상치 방치 / 축 왜곡 / 구간 자르기 / 과잉일반화
- **그래프는 창 하나에 탭 4개** (개별 차트를 늘어놓지 않는다 — 분량 관리)
- **축 왜곡 탭만 막대그래프.** 선그래프보다 막대가 y축 절단의 왜곡을 훨씬 분명히 보여준다.
- 구간 강조 음영은 `--tide-dim` 계열 밝은 파랑.
- **네 그래프 모두 실제 관측 자료가 아니라 설명용 임의 데이터**이며, 그 사실을 차트 상단에 명시.
  각 실수를 가장 잘 보여주는 형태로 값을 설계(예: 이상치는 레버리지가 큰 왼쪽 끝에 배치).
- 임의 데이터는 결정적 의사난수(`s2demoLine`의 seed)라 **새로고침해도 같은 그림**이 나온다.

### 8-6. 검증 결과 (Chromium/Playwright)

- 데스크톱 1280px · 모바일 390px 모두 가로 스크롤 없음
- `채우기` → `불러오기` 실제 fetch 성공(146개/67개 연도)
- 계산값이 Python(pandas·numpy) 결과와 일치(r=0.97, a=0.0083, R²=0.77/0.91)
- 네트워크 차단 시 실패 문구 → `저장된 데이터로 보기` → 동일 결과 + 배지 전환 정상
- 04 토글 2종, 07 토글 4종, 확인 문제 3개 정상. 콘솔 오류 없음

---

## 9. step3.html — 웹앱 제작 연습

### 9-1. 페이지 성격

- 5차시 전에 AI 코딩 도구로 짧은 웹앱을 한 번 만들어보는 **연습.**
  **일괄 제출을 받지 않는다**(지침 4번 제출 3종에 미포함). 재미있게 만든 학생만 **희망**으로
  Teams 개인 채팅을 통해 선생님에게 전달하고, 전달된 웹앱은 **전교생이 볼 수 있도록 게시된다**
  (§5-3 참고).
- 디자인 톤은 **안내 웹앱 디자인 시스템과 통일할 필요 전혀 없다.** 학생 자율.
- AI 코딩 도구는 자유 선택. 특정 툴을 지정하지 않는다.
  **유료/무료 성능 차이는 페이지에 언급하지 않는다**(사전 설문 정보는 교사 내부 참고용).
- 소요시간 `약 20분`. `.pagenav`: `← 데이터 분석 방법` / `메인으로` / `데이터 활용 가이드 →`

### 9-2. 섹션 구성 (8개)

| # | 섹션 | 핵심 |
|---|---|---|
| 01 | 오늘 할 일 | 목적 안내 + `.flowsteps` 4단계 |
| 02 | 사용할 데이터 | 예시/실제 선택 카드(`.datapick`) → 선택 시 데이터 로드. **예시·실제 모두 확인 버튼 옆에 CSV 다운로드 버튼 제공**(위치 통일) |
| 03 | 데이터 미리보기 | 20개 필드 전체 나열 표, 필드별 응답 분포를 **누적 막대(%) + 숫자**로 표시. **표 아래에 CSV를 AI에 첨부해 상관관계·웹앱 소재를 물어보는 고정 프롬프트 추가** |
| 04 | 프롬프트 예시 보기 | 교사 예시(**`class` × `ei`, 반별 E/I 비율 100% 누적 세로 막대**), 그래프 박스 **안**에 자동 계산되는 한 줄 코멘트 포함(최다 E/I 반 비교). 공통 규칙은 연하게·개별 내용은 tide 강조 |
| 05 | 프롬프트 만들기 | 학생이 직접: 문항 체크박스(**`class` 포함**, 개수 제한 없음, "2~5개 내외 추천"만 안내) + 보여주는 방식 + 디자인 무드 → 복사. **또는, 03에서 이미 AI와 대화하며 주제를 정했다면 "공통 규칙만 복사" 버튼으로 규칙만 이어 붙일 수 있다** |
| 06 | 다시 대화해서 수정하기 | AI와 대화하며 다듬는 과정 안내 |
| 07 | 재미있게 만들었다면 | **Teams 개인 채팅으로 선생님에게 파일 전달 → 전교생 공개 게시** |
| 08 | 다음 단계 | step4 예고 |

### 9-3. 데이터 아키텍처

**예시 데이터 — "CSV 파일 업로드" 방식**

- 160명 데이터를 `script.js`에 정적 내장(`STEP3_EXAMPLE_HEADERS`, `STEP3_EXAMPLE_ROWS`)
  → 03섹션 미리보기가 오프라인에서도 동작.
- 02섹션 **"⬇ CSV 파일 다운로드" 버튼** — 클릭 시 브라우저가 즉석에서 `example_data.csv`를 생성해
  내려받는다(`step3ExampleCSV()` + `step3DownloadExampleCSV()`, **BOM 포함**해 엑셀 한글 깨짐 방지).
- 04·05 프롬프트의 "데이터 출처" 문구: "미리 다운로드한 CSV 파일(`example_data.csv`)을 AI 코딩
  도구에 첨부해서 전달하라" + 헤더(열 이름) 목록 텍스트.
  **fetch·CSV 원문 텍스트는 프롬프트에 넣지 않는다.**
- `EXAMPLE_CSV_URL` 상수는 교사가 원본 시트를 빠르게 열어보는 용도로만 남아 있고,
  **프롬프트에서는 쓰이지 않는다.**

**실제 데이터 — 02는 "비밀번호로 보호된 API", 05 프롬프트는 "CSV 정적 내장"**

02섹션(인증·미리보기)과 05섹션(학생 프롬프트)은 데이터를 다루는 방식이 서로 다르다.

- **02 인증**: 비밀번호 기반 API 방식. **원본 스프레드시트 링크는 어디에도 노출되지 않는다.**
  Google Apps Script를 데이터 시트에 배포(`REAL_APPS_SCRIPT_URL`). `doGet(e)`가
  `e.parameter.password`를 검사, 일치할 때만 JSON을 반환한다. `CacheService`로 1시간 캐시
  (`CACHE_SECONDS = 3600`, 동시 접속 20명 대비). 비밀번호 입력 → fetch 성공 시 03섹션 미리보기
  까지 함께 갱신되고, **확인 버튼 바로 옆에 CSV 다운로드 버튼이 나타난다**(비밀번호 입력창은
  폭 축소).
- **05 프롬프트("공통 규칙")**: 예시 데이터와 같은 패턴을 따른다 — 02에서 받은 CSV
  (`real_data.csv`)를 AI 코딩 도구에 첨부하고, 그 내용을 코드 안에 정적으로 포함시키라고
  지시한다. 데이터 출처 블록에 Apps Script URL도 비밀번호도 등장하지 않는다. 헤더(열 이름)
  목록은 `STEP3_STATE.rows`에서 그때그때 뽑아 넣는다.
  ⚠️ **"우리 반"이라고 쓰면 AI가 학급 단위 데이터로 오인해 반 전용 웹앱을 만든다.** 반드시
  "전교생"으로 쓴다 — 데이터 출처 문장에 이미 반영돼 있다.
- `REAL_APPS_SCRIPT_URL`은 **02 인증에만 쓰인다. 05가 만드는 프롬프트에는 노출되지 않는다.**

**04섹션 결과 예시 그래프**

- `class`(반) × `ei`, 반별 E/I 비율을 **100% 누적 세로 막대**로 그린다. 8개 반이 x축, 각 막대
  안에서 E(아래)·I(위) 비율만큼 색이 나뉜다.
- 텍스트 설명이 아니라 실제 SVG 막대그래프(`step3RenderExampleChart`).
- **02에서 선택한 데이터(`STEP3_STATE.rows`)를 그대로 사용**한다. 실제 데이터를 선택했는데
  응답이 적으면 그래프도 그만큼 작게 나오는 것이 정상 동작이다.
- 주의 문구는 그래프 박스 안 `.chart-note`에 통합한다. E 비율이 가장 높은 반과 I 비율이 가장
  높은 반을 실제 데이터로 계산해 "○반은 E 비율이 가장 높고(○%), ○반은 I 비율이 가장
  높습니다(○%)" 형태의 한 줄 코멘트를 자동 생성하고, 마지막 문장에 표본 크기 주의(반당 20명
  안팎이라 비율이 쉽게 흔들린다)를 포함한다. 학생 웹앱에도 이런 코멘트가 붙으면 좋다는 본보기
  역할을 겸한다.

### 9-4. [교사 수정 구역] — 배포 전 반드시 확인

```javascript
const REAL_APPS_SCRIPT_URL = "https://script.google.com/macros/s/AKfycb.../exec";
const EXAMPLE_CSV_URL = "https://docs.google.com/.../pub?...&output=csv"; // 참고용, 프롬프트 미사용
```

- Apps Script `PASSWORD`(현재 `0000`)를 실제 값으로 변경할 것.
  `script.js`는 안 건드려도 된다(학생이 화면에서 입력, 서버만 검증).
- `SHEET_NAME`이 실제 시트 탭 이름과 일치하는지 확인.

**Apps Script 최종 코드**

```javascript
var PASSWORD = '0000';
var SHEET_ID = '1FhiQyDN1OkGbJIMDqMLTByZrW5l6FhO6au-AMy3iVhQ';
var SHEET_NAME = '데이터';
var CACHE_SECONDS = 3600;

function doGet(e) {
  var password = (e && e.parameter && e.parameter.password || '').trim();
  if (password !== PASSWORD) return jsonResponse({ ok: false, error: 'wrong_password' });
  try {
    var cache = CacheService.getScriptCache();
    var cached = cache.get('real_data');
    if (cached) return jsonResponse(JSON.parse(cached));
    var sheet = SpreadsheetApp.openById(SHEET_ID).getSheetByName(SHEET_NAME);
    if (!sheet) return jsonResponse({ ok: false, error: 'sheet_not_found' });
    var values = sheet.getDataRange().getValues();
    var headers = values[0];
    var rows = values.slice(1)
      .filter(function (row) { return row.join('') !== ''; })
      .map(function (row) {
        var obj = {};
        headers.forEach(function (h, i) { obj[h] = row[i]; });
        return obj;
      });
    var result = { ok: true, count: rows.length, data: rows };
    cache.put('real_data', JSON.stringify(result), CACHE_SECONDS);
    return jsonResponse(result);
  } catch (err) {
    return jsonResponse({ ok: false, error: 'server_error', message: String(err) });
  }
}
function jsonResponse(obj) {
  return ContentService.createTextOutput(JSON.stringify(obj)).setMimeType(ContentService.MimeType.JSON);
}
```

배포 URL: `https://script.google.com/macros/s/AKfycbwGSbvkSlKqDqBYo1oqgv_bMzHbYy3xQE_3xGqedwT0r38N0YMV90BshGnKOIYlLdfL/exec`

### 9-5. 프롬프트 안전장치 (고정 삽입)

1. `STEP3_SECURITY` 상수에는 **지침 5의 1·2번 문구**가 그대로 담겨 있다(§5-3 참조). 3번(하드코딩
   금지)은 비밀번호를 매번 학생이 입력하므로, 5번(저장 기능 금지)은 저장 기능을 쓰지 않으므로
   실질적으로 지켜진다. 4번(정적 데이터만 사용)은 데이터 출처 블록의 "외부 링크나 API를
   호출하지 않는다"는 문장으로 흡수된다. 결과적으로 05가 만드는 공통 규칙 전체를 보면 지침 5
   원문 5개 항목이 실질적으로 모두 반영돼 있다.
2. **"값은 헤더(필드) 이름으로 찾는다. 문항이 빠지거나 순서가 바뀌어도 이름으로 찾도록
   코드를 짠다"** — 나중에 실제 설문 데이터로 교체되거나 문항이 빠져도 코드가 깨지지 않게
   하는 안전장치. 예시·실제 데이터 출처 블록 양쪽에 **반드시 포함.**
3. 출력 파일 형식은 **`웹앱제목.html`**이다(예: 웹앱 제목이 "우리 학교 민초 논쟁"이면 →
   `우리학교_민초논쟁.html`). 배포 방식이 OneDrive에 파일을 올려 열람하는 방식이라 GitHub
   Pages 배포를 전제로 한 `index.html` 고정이 필요 없다. **step5·step6(정식 웹앱)은 여전히
   GitHub Pages 배포를 전제로 `index.html`을 쓴다**(`학생용_프롬프트-확정사항.md` 참고).
4. 03섹션에는 **"공통 규칙만 복사" 진입점**이 있다. 03에서 AI와 대화하며 이미 주제를 정한
   학생은, 규칙 본문(역할→맥락→데이터 출처→출력 형식→보안)에 요청 항목을 자동으로 맨 뒤에
   붙여 복사한 뒤 그 대화에 이어 붙일 수 있다. 문항을 채워 처음부터 새 프롬프트를 만드는
   기존 경로(05 "복사하기")와 두 경로가 공존한다.
5. 03·04·05의 모든 복사 버튼은 `copyTextToClipboard()`(§4-4)를 공유한다.

---

## 10. 설문 · 예시 데이터셋 (step3 실습 자료)

### 10-1. 최종 설문 문항 (MS Forms, 20문항)

> 28문항 초안이 있었으나 **최종본은 20문항.** 이후 모든 참조는 이 표 기준.

| # | 카테고리 | 문항 | 필드명 | 선택지 값(원문 그대로) |
|---|---|---|---|---|
| 1 | MBTI | E/I | `ei` | E / I |
| 2 | MBTI | N/S | `ns` | N / S |
| 3 | MBTI | T/F | `tf` | T / F |
| 4 | MBTI | P/J | `pj` | P / J |
| 5 | 음식 | 짜장면 / 짬뽕 | `jjajang_jjambbong` | 짜장면 / 짬뽕 |
| 6 | 음식 | 탕수육 찍먹 / 부먹 | `tangsuyuk` | 찍먹 / 부먹 |
| 7 | 음식 | 후라이드 / 양념 | `chicken` | 후라이드 / 양념 |
| 8 | 음식 | 물복 / 딱복 | `peach` | 물복 / 딱복 |
| 9 | 음식 | 민초 / 반민초 | `mintchoco` | 민초 / 반민초 |
| 10 | 일상 | 강아지 / 고양이 | `pet` | 강아지 / 고양이 |
| 11 | 일상 | 산 / 바다 | `mountain_sea` | 산 / 바다 |
| 12 | 일상 | 아침형 / 저녁형 | `chronotype` | 아침형 / 저녁형 |
| 13 | 일상 | 더 힐링되는 것은? | `healing_style` | 친구들과 다같이 놀기 / 혼자 자유시간 보내기 |
| 14 | 일상 | 선호하는 여행 스타일은? | `travel_style` | 관광지에서 부지런히 돌아다니기 / 휴양지에서 마음 편히 휴식하기 |
| 15 | 일상 | 고민상담하고 싶은 친구는? | `counsel_style` | 다정하게 경청하고 위로해주는 친구 / 좋은 해결책을 제시해주는 친구 |
| 16 | 학업 | 가장 좋아하는 과학 분야 | `subject` | 물리학 / 화학 / 생명과학 / 지구과학 |
| 17 | 학업 | 시험 전날 더 가까운 타입은? | `exam_style` | 밤새 벼락치기 / 일찍 숙면하기 |
| 18 | 학업 | 더 끌리는 미지의 영역은? | `frontier` | 우주 탐사 / 심해 탐사 |
| 19 | 학업 | 두 직업 중 선택해야 한다면? | `career_choice` | 과학자 / 공학자 |
| 20 | 학업 | 모둠 과제에서 선호하는 역할은? | `group_role` | 앞에 나서서 발표하기 / 뒤에서 차분히 자료조사 |

설문에는 없지만 정제 시 추가하는 필드: `class` — 학급, 값 1~8 (반별 20명, 총 160명)

**필드명 규칙**: 전부 영어. 학생 웹앱 코드에서 변수명·CSV 헤더로 그대로 쓰인다.
**선택지 값은 MS Forms 원문과 글자 단위로 일치해야 한다**(예: "벼락치기"가 아니라 "밤새 벼락치기").
문자열 정확 일치로 분포를 집계하므로 표기가 조금만 달라도 같은 항목이 다른 값으로 쪼개진다.

### 10-2. 사전 설문(설문①) 구성

MS Forms 페이지 순서: **1페이지 사전 점검·자기진단 → 2페이지 데이터셋용(10-1 표).**

- **A~E (사전 점검)**: 노트북 보유, 생성형 AI 사용 현황(주로 쓰는 AI·유료 여부·제작 경험),
  프로그래밍·엑셀·HTML 경험, Teams 숙련도, 우려사항(자유서술)
- **F~I (역량 자기진단, 5점 척도)**: 데이터 분석 4문항 / AI 활용 3문항 / 과학적 의사소통 2문항 /
  종합 자신감 1문항. **사전·사후 동일 문항으로 재질문해 효과 검증에 사용**(AI 선도학교 사례 점검).
  도입부에 "수업 전후 비교를 위해 동일하게 재질문 예정"임을 명시하는 안내문 포함.
- 기숙학교라 "학교 외 공간 인터넷 연결" 문항은 삭제.

### 10-3. 예시 데이터셋에 심어둔 상관관계

160명 예시 데이터셋 생성 시 아래 쌍에 **느슨한 경향**을 의도적으로 심었다(결정론 아님).
step2의 "상관≠인과"를 자기 데이터로 다시 확인시키려는 의도이며, 04·05 프롬프트 힌트로 활용 가능.

| 독립변수 | 종속변수 | 심어둔 경향 |
|---|---|---|
| `ei` | `group_role` | E → "앞에 나서서 발표하기" 68~72%, I → "자료조사" |
| `ei` | `healing_style` | E → "다같이 놀기", I → "혼자 자유시간" 70~72% |
| `tf` | `counsel_style` | F → 공감형, T → 해결책형 70~75% |
| `pj` | `exam_style` | J → "일찍 숙면하기", P → "밤새 벼락치기" 68~70% |

나머지 필드(음식 취향, 반려동물, 산/바다, 과목, 진로 등)는 독립 생성이며 의도된 상관이 없다.

### 10-4. 산출 파일 (완성, 사용자 보관 중)

1. `설문2_데이터응답_서식.xlsx` — 실제 응답 정리용 서식(안내 시트 + 필드명 매핑표 + 응답데이터_서식).
   **실제 응답을 받은 뒤 사용.**
2. `step3_예시데이터셋_160명.xlsx` — 10-1 구조 그대로 8개 반×20명=160행 가상 응답
   (시드 42 고정, 재현 가능). `데이터` 시트 + `필드 설명` 시트.
   → **현재는 이 데이터가 `script.js`에 정적 내장되어 있으므로 별도 링크가 필요 없다.**

---

## 11. 미결 사항 (통합)

| # | 항목 | 관련 |
|---|---|---|
| 1 | **⚠️ 실제 데이터로 만든 웹앱이 전교생에게 게시될 경우, 웹앱 코드에 정적으로 내장된 설문 응답 원본(CSV)도 함께 노출된다.** 이름·이메일은 없지만 전교생 실제 응답이 그대로 공개되는 셈이라, 개인정보보호 관점에서 이대로 둘지·예시 데이터만 게시 허용할지·다른 안전장치를 둘지 결정이 필요하다. **코딩으로 풀 문제가 아니라 배포 범위 자체에 대한 정책 결정.** | step3 (§5-3 참고) |
| 2 | Apps Script `PASSWORD` 변경 (현재 `0000`) | step3 |
| 3 | 실제 데이터 스프레드시트에 실제 응답 채우기 — 채워지면 자동 반영(코드 수정 불필요) | step3 |
| 4 | `step3_예시데이터셋_160명.xlsx` 파일의 `counsel_style` 옵션 문구를 최신값("다정하게 경청하고 위로해주는 친구")으로 동기화 — 코드(`script.js`) 쪽은 이미 반영됨 | step3 |
| 5 | `SUBMIT_SCHEDULE` 반별 제출 기한 입력 (현재 전부 `null` → "미정") | step1 |
| 6 | step1.html 실물의 루브릭 표를 §5-1(15점/5개 영역) 기준으로 갱신 | step1 |
| 7 | 허브 "결과 공유" 2카드 — 학생 결과물 배포 후 실제 링크로 교체 | index |
| 8 | `outputs/` 폴더명 규칙 확정 — 개인 식별자 기반(예: `반-학번-주제`)으로 변경 | §5-5 |
| 9 | 개별 전환 후 주제 중복 제한 여부 결정 — "반별 최대 2개까지 같은 주제 허용" 규칙을 유지할지, 완전 자유 선택으로 둘지 | §5-5 · step4 |
| 10 | step4·6 제작 (step5은 §13으로 1차 작성 완료) | — |
| 11 | **§5-4 재확인 필요** — §5-4는 "관찰-연결-임팩트-요청 4단계는 step4에서 처음 소개, step5는 그걸 적용"을 전제하지만, step4가 미착수라 step5(§13)는 우선 4단계를 처음부터 다시 설명하는 방식으로 1차 작성했다. step4 제작 시 프레임워크를 거기서 먼저 가르칠지, 계속 step5에서 전담할지 확정하고 §5-4·§13을 맞출 것 | §5-4 · §13 |
| 12 | step4 제작 시 `styles.css` 35번 섹션(`.topicgrid` 등) 재사용 여부 확인 — 그대로 쓸지, 새로 설계할지 | §3-3 · step4 |
| 13 | step5 제출 기한 미표시 결정이 다른 step에도 적용되는지 — step5는 `SUBMIT_SCHEDULE`/`data-due`를 의도적으로 쓰지 않기로 했는데(§13-4), step1 제출물 표 등 기존 페이지의 기한 표시 방식과 방향이 달라졌으므로 전체적으로 맞출지 검토 | §13-4 · §4-1 |

**배포 전 눈으로 확인할 것**

- step3 실제 데이터 프롬프트 문구가 "반 친구들끼리만" / "우리 반 전용"이 아니라 **"전교생"**으로 되어 있는지
- step3 공통 프롬프트("공통 규칙만 복사" 결과 포함)에 **Apps Script 링크·비밀번호 문자열이
  전혀 없고**, `real_data.csv`로만 표기되는지
- 02섹션 비밀번호 확인 성공 시 CSV 다운로드 버튼이 **확인 버튼 옆에** 나타나는지(아래 아님)
- 07 문구가 실제 게시 방침(전교생 공개)과 일치하는지 — 방침이 아직 확정 전이라면(§11-1)
  화면 문구를 수정 전 상태로 되돌릴지도 함께 검토

---

## 12. 폐기된 결정 (다시 착각하지 말 것) ⚠️

| 폐기된 내용 | 최종 결정 |
|---|---|
| `script.js`는 step3에서 최초 생성 | **step1에서 생성됨** |
| `styles.css` 반응형·모션·인쇄 = 19~21번 | **26~28번** (전체 1~35, §3-3) |
| 루브릭 대분류 5개 (배점 없음) | **명칭 일부 변경 + 배점 신설, 15점** — 한때 6개 영역·20점(동료평가 포함)이었다가 최종 5개 영역·15점으로 축소 (§5-1) |
| 모둠 구성: 2~3인 모둠 (지침 4번) | **개별 수행평가로 전환** — 160명 전원이 각자 기획·제작·제출 (§5-5) |
| 지침 5(데이터·보안 원칙) 예외 범위 = 5개 항목 전부 | **02 인증 로직의 4번 항목 하나로 축소** (§5-3) |
| step2 첫 프로토타입: 정적 SVG를 미리 구워 넣기 | **브라우저에서 실시간 CSV fetch → 계산·렌더링.** 이후 step에서도 이 방식이 기본 |
| step2에 "상관≠인과" 별도 섹션 | **04 상관분석 섹션 끝에 소제목으로 흡수** (r을 본 직후 경고가 오는 흐름) |
| step2 회귀 소재 = CO₂ → 기온 | **연도 → 기온.** CO₂로 회귀하면 04 상관의 연장선처럼 보여 회귀의 핵심인 **예측**이 살지 않는다. 05에는 CO₂가 아예 등장하지 않음 |
| "선형회귀" 섹션 | **"회귀"**로 확장, 다항(2차) 포함. 차수 선택 기준(R² 상승 폭 + 예측구간 요동 = 과적합)까지 다룸 |
| 사이킷런 언급 | 삭제 |
| MathJax·KaTeX 도입 | **미도입**, `.formula` 클래스로 처리 (§4-5) |
| step3 예시 데이터를 구글 시트 "웹에 게시(CSV)"로 fetch | **폐기 — CORS 차단**(`Access-Control-Allow-Origin` 헤더 없음, "Failed to fetch"). AI가 코드 작성 전 파일을 열어보려는 시도도 robots.txt에 막힘. → **CSV 다운로드 후 AI에 첨부** 방식으로 전환하며 두 문제 모두 소멸 |
| step3 프롬프트에 CSV 전문을 통째로 삽입 | **되돌림** — 정보량 과다 |
| step3 04섹션 그래프를 항상 내장 예시 데이터로 그림 | **02에서 선택한 데이터로 그림** |
| step3 03섹션 = "AI 코딩 도구" | **"데이터 미리보기"** |
| step3 04섹션 = 기본형·응용형 프롬프트 2종 | **04 교사 예시 1종 + 05 학생 프롬프트 빌더** |
| step3 실제 데이터를 "오픈소스"로 표현 | 사용 금지 (§2-3) |
| step3 프롬프트에 "우리 반" | **"전교생"** (여전히 유효 — 데이터 출처 문구에 남아 있음) |
| step3 실제 데이터를 05 프롬프트에서 "비밀번호 포함 링크"로 직접 전달 | **CSV로 다운로드해 예시 데이터와 동일하게 정적 내장하는 방식으로 통일.** §5-3 예외 범위도 함께 축소됨 |
| step3 04 예시 = `ei` × `group_role`, 가로 막대(모둠 역할 선호) | **`class` × `ei`, 반별 E/I 비율 — 100% 누적 세로 막대로 교체** |
| step3 04 그래프 아래 "그런데 이 차이만으로…" 경고를 별도 `.card`로 배치 | **그래프 박스 안 `.chart-note`로 통합.** 최다 E/I 반을 실제로 계산해 코멘트를 자동 생성 |
| step3 05 출력 형식 = `index.html` 고정 | **`웹앱제목.html`로 변경** (예: `우리학교_민초논쟁.html`) — OneDrive 업로드·열람 방식이라 `index.html` 강제가 불필요해짐. step5·step6 정식 웹앱에는 이 변경 미적용 |
| step3 07 "Teams `지구과학` 채널에 희망자만 공유, 외부 공개 배포 없음" | **"Teams 개인 채팅으로 선생님에게 전달 → 전교생 공개 게시"로 변경** (§5-3·§11-1에 후속 검토 필요 항목으로 기재) |
| step3 복사 버튼 = `navigator.clipboard.writeText`만 사용 | **`execCommand("copy")` 대체 경로 추가**(`copyTextToClipboard()`, §4-4) — 보안 컨텍스트가 아닌 환경(OneDrive 미리보기 등)에서 조용히 실패하던 사고를 계기로 전면 교체 |
| step3 05 `fieldpicker` = 20개 설문 문항만 | **`class`(반) 추가 — "기본정보" 카테고리 신설**, 04·05 양쪽에 반영 |
| `counsel_style` 옵션 "내게 잘 공감하고 위로해주는 친구" | **"다정하게 경청하고 위로해주는 친구"로 변경** (MS Forms 원본 수정에 맞춰 코드·예시 데이터 동기화) |
| step3 02 실제 데이터 안내 문구 "웹 링크로 데이터를 불러오는 연습" | **"CSV로 내려받아 첨부하는 연습"으로 통일** (예시 데이터와 같은 문구 패턴) |
| step3 02 CSV 다운로드 버튼 = 비밀번호 줄 아래 별도 배치 | **확인 버튼 바로 옆으로 이동**, 비밀번호 입력창 폭 축소(116px) |

---

## 13. step5.html — 기획안 작성

**페이지 목적**: 관찰-연결-임팩트-요청으로 전달할 메시지를 정리하고, AI 코딩 도구에 줄 프롬프트(1단계 공통 규칙 + 2단계 개별화 틀)를 완성한다. 실제 기획안 제출은 **MS Teams 과제**로 받는다 — 이 페이지는 화면 안내일 뿐 제출 기능은 없다.

소요시간 `약 25분`. `.pagenav`: `← 데이터 활용 가이드` / `메인으로` / `웹앱 구현 및 점검 →`

> ⚠️ **§5-4와의 관계 미확정** — §5-4는 프레임워크를 step4에서 먼저 소개한다고 되어 있지만, step4가 미착수라 아래 02섹션은 우선 처음부터 다시 설명하는 내용으로 작성했다. §11-11 참고.

### 13-1. 섹션 구성 (6개)

| # | 섹션 | 핵심 내용 |
|---|---|---|
| 01 | 오늘 할 일 | 목적 안내 + `.flowsteps` 4단계(프레임워크 정리 → 프롬프트 1단계 확인 → 프롬프트 2단계 작성 → 팀즈 과제 제출) |
| 02 | 메시지 설계 프레임워크 | 관찰·연결·임팩트·요청 4단계. 각 항목: 질문 + 기온·CO₂ 예시(모두 한 문장) + 여러 줄 입력창(저장 안 됨, `.msgframe`) |
| 03 | 프롬프트 1단계 — 공통 규칙 | `.codeblock`으로 고정 문구 노출. "(반드시 지킬 것)" 표시 항목(출력 형식·데이터·보안 원칙) 외에는 학생이 자신의 기획에 맞게 수정 가능하다고 `.note.tide`로 안내 |
| 04 | 프롬프트 2단계 — 개별화 틀 | 빈 서식(대괄호) `.codeblock` + "작성 예시 보기" 토글(`.exwrap`, 기본 접힘, 기온·CO₂ 예시 전문) + "제시된 예시를 참고해서 MS Teams 과제에서 내용을 작성합니다." 한 줄 안내. 입력창 없음(서술량이 많아 웹앱 내 작성 비효율) |
| 05 | 기획안 제출 | "MS Teams 과제로 제출" 명시 + Part A(메시지 설계 4개)·Part B(웹앱 기획 명세 7개) 체크리스트. 정적 목록(클릭 인터랙션 없음) |
| 06 | 다음 단계 | "완성한 프롬프트를 그대로 들고 다음 순서로 AI 코딩 도구와 대화를 시작한다" — 차시 표현("다음 시간", "step6에서") 쓰지 않음 |

### 13-2. 기획안(Part B) 항목 구성 — 프롬프트 2단계와 1:1 대응

| # | 항목 |
|---|---|
| 1 | 배경과 목표 |
| 2 | 대상 |
| 3 | 데이터 명세 |
| 4 | 기능 명세 |
| 5 | 수락 기준 |
| 6 | 리스크 |
| 7 | 디자인(선택) |

Part A(관찰·연결·임팩트·요청)를 앞에 별도 항목으로 뺀 이유: 04 프롬프트 서식에서는 기능 명세(4번) 안에 묻혀 눈에 안 띄어서, 기획안에서는 독립시켜 루브릭(§5-1 의사소통 효과성) 대응 지점을 명확히 했다.

### 13-3. 평가 단위 — 개별 과제 반영

§5-5(모둠 → 개별 전환)에 맞춰 페이지 전체 문구를 개별 기준으로 썼다.

- 프롬프트 1단계 "앞으로의 진행 방식": "모든 학생에게 공통으로 적용되는 기본 규칙" / "내가 만들 웹앱"
- 프롬프트 2단계 서식 제목: `[내가 만들 웹앱 — 세부 내용]`
- 사용자 맥락 항목에 "기획 내용은 사용자 요청을 정확히 반영, 구체적 구현 방식은 AI가 판단해도 됨"을 명시 — 학생이 웹 개발 경험이 많지 않다는 전제를 프롬프트 자체에 반영

### 13-4. 제출 기한·차시 표현을 의도적으로 뺌

- **`data-due`/`SUBMIT_SCHEDULE` 연동 없음.** 실제 진행이 유동적이라 차시를 딱딱하게 나누지 않기로 했고, 화면에 마감일을 보여주지 않는다. §11-13에 다른 step과의 통일 여부가 미결로 남아 있다.
- **"다음 시간", "step6에서" 같은 차시 지칭 표현을 쓰지 않는다.** "다음 순서로"처럼 흐름만 표현. 실제 차시 분절은 교사가 진행하며 조절한다.

### 13-5. 프롬프트 커스터마이즈 허용 — 새 원칙

1단계 공통 규칙 안내에 **"(반드시 지킬 것)"이 아닌 항목(역할·사용자 맥락·대상과 태도·진행 방식)은 학생이 자유롭게 수정해도 된다"**는 문구를 추가했다. 능숙한 학생은 자기 상황에 맞게 프롬프트를 고쳐 쓰도록 유도하는 것이 목적 — 프롬프트 작성 능력 자체를 가르치려는 의도. 이후 step3·step4 프롬프트 안내에도 같은 원칙을 적용할지 검토할 만하다(§11 신규 항목으로 추가하지 않음 — 필요 시 별도 논의).

### 13-6. 구현 메모

- 신규 CSS: `styles.css` 섹션 36(`.msgframe`, `.exwrap`) — §3-3 참고.
- 신규 JS 없음. 복사 버튼은 `initCopyButtons()`(§4-4) 재사용, "작성 예시 보기"는 네이티브 `<details>/<summary>`라 JS 불필요.
- `.check` 컴포넌트는 텍스트를 감싸는 래퍼 `<div>` 하나를 `::before`(체크박스) 다음 두 번째 자식으로 둬야 `strong`+`span`이 세로로 쌓인다(`<div class="check"><div><strong>…</strong><span>…</span></div></div>`). 문서에 마크업 예시가 없어 CSS만 보고 새로 정한 규칙이니, 이후 step에서도 이 구조를 따를 것.
- `학생용_프롬프트-확정사항.md`와 대조해 1차 작성본의 누락 2건을 복원함: ① 2단계 서식 "데이터 명세"의 "(참고: 실제 데이터 파일은 이 메시지에 이어서 첨부하거나 붙여넣는다)", ② 서식 맨 끝의 "질문→요약→확인" 마무리 지시문(3문장). 새 step 대화를 열 때는 반드시 `학생용_프롬프트-확정사항.md` 원문과 한 줄씩 대조할 것 — 요약해서 옮기면 이런 누락이 생긴다.
- 03섹션 "사용자 맥락"에 `학생용_프롬프트-확정사항.md` 원문에는 없는 문장을 추가했다: "사용자는 웹 개발 경험이 많지 않다"는 전제와 "기획은 최대한 반영하되 구체적 구현 방식은 AI 판단에 맡겨도 된다"는 위임 문구. `학생용_프롬프트-확정사항.md` 자체는 아직 갱신 안 됨 — 원본 문서도 이 변경을 반영할지 별도 확인 필요.
